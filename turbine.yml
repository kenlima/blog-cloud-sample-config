server:
  port: ${PORT:8999}

management:
  port: 8999
#turbine:
#  aggregator:
#    clusterConfig: COMPOSITE, EDGE-SERVER
#  appConfig: composite, edge-server
#  clusterNameExpression: new String("default")
